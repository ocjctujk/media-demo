import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.maharshi.com',
  appName: 'Media Demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
